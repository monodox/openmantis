{
	"name": "openmantis",
	"version": "0.0.0",
	"private": true,
	"description": "OpenMantis — Node.js and Python SDKs for local-model orchestration.",
	"workspaces": [
		"packages/node"
	],
	"repository": {
		"type": "git",
		"url": "https://github.com/monodox/openmantis.git"
	},
	"scripts": {
		"test": "npm test --workspace=packages/node",
		"test:node": "npm test --workspace=packages/node",
		"test:python": "cd packages/python && python -m pytest",
		"test:all": "npm run test:node && npm run test:python"
	},
	"license": "MIT"
}
